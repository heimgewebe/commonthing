---
apiVersion: batch/v1
kind: Job
metadata:
  name: commonthing-experiment-b-migration
  namespace: commonthing-experiment-b
  labels:
    app.kubernetes.io/name: commonthing-experiment-b-migration
    app.kubernetes.io/component: database-migration
spec:
  backoffLimit: 4
  activeDeadlineSeconds: 480
  template:
    metadata:
      labels:
        app.kubernetes.io/name: commonthing-experiment-b-migration
        app.kubernetes.io/component: database-migration
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: commonthing-experiment-b-registry
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migration
          image: ghcr.io/heimgewebe/commonthing-api@${API_DIGEST}
          imagePullPolicy: IfNotPresent
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: weltgewebe-runtime
                  key: database-url
            - name: APP_BASE_URL
              value: http://commonthing-experiment-b.invalid
            - name: AUTH_AUTO_PROVISION
              value: "0"
            - name: AUTH_COOKIE_SECURE
              value: "0"
            - name: AUTH_LOG_MAGIC_TOKEN
              value: "0"
            - name: AUTH_PUBLIC_LOGIN
              value: "0"
            - name: GEWEBE_IN_DIR
              value: /data
            - name: GEWEBE_SEED_DEMO
              value: "false"
            - name: GEWEBE_SEED_REAL
              value: "false"
            - name: RUST_LOG
              value: info
            - name: WELTGEWEBE_API_MIGRATION_ONLY
              value: "1"
            - name: WELTGEWEBE_API_STARTUP_MIGRATIONS
              value: run
            - name: WELTGEWEBE_DOMAIN_ACCOUNT_WRITE_SOURCE
              value: postgres
            - name: WELTGEWEBE_DOMAIN_EDGE_WRITE_SOURCE
              value: postgres
            - name: WELTGEWEBE_DOMAIN_NODE_WRITE_SOURCE
              value: postgres
            - name: WELTGEWEBE_DOMAIN_READ_SOURCE
              value: postgres
            - name: WELTGEWEBE_PASSKEY_CREDENTIAL_SOURCE
              value: postgres
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            privileged: false
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          emptyDir: {}
        - name: tmp
          emptyDir: {}